---
description: Maps incoming work to the right skill workflow and defines shared operating rules
---

Use the `using-agent-skills` meta-skill.

Use when starting a session or deciding which skill applies.