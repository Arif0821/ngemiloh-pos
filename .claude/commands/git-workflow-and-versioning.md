---
description: Trunk-based development, atomic commits, change sizing (~100 lines), the commit-as-save-point pattern
---

Use the `git-workflow-and-versioning` skill.

Use when making any code change (always).